import { Container } from "./styles";

import { ScrollView } from "react-native";

export function BodyTikTok() {
  return (
    <Container>
   <ScrollView horizontal={true} showsHorizontalScrollIndicator={false}>
   
   </ScrollView>
    </Container>
  );
}